# 숫자 카드 게임
# 난이도 1 | 시간제한 1초 | 메모리 제한 128MB | 기출 2019 국가 교육기관 코딩 테스트

N, M = map(int, input().split())

result = 0
for row in range(N):
  cards = list(map(int, input().split()))
  min_val = min(cards)

  result = max(result, min_val)
print(row)
